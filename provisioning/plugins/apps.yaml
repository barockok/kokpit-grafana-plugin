apiVersion: 1

apps:
  - type: 'kokpit-kokpitslo-app'
    org_id: 1
    org_name: 'kokpit'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
